# 缘分故事屋 - 部署说明

前后端完整站点，**零数据库依赖**（数据存 JSON 文件），两种部署方式任选其一。

## 项目文件

| 文件 | 说明 |
|------|------|
| index.html | 前端首页（故事广场 / 详情弹窗 / 评论 / 关于） |
| publish.html | 独立发布页（纯文本 / HTML 编辑、上传 HTML 文件、富文本粘贴） |
| admin.html | 站长管理页（反馈箱 + 故事管理，需管理密码） |
| feedback.html | 反馈建议页（网友提交，存入 data/feedback.json） |
| terms / privacy / disclaimer.html | 内容规范 / 隐私声明 / 免责声明 |
| api.php | PHP 后端（生产用，宝塔/Docker） |
| server.js | Node 本地开发服务器（接口与 api.php 对齐，仅本地测试用） |
| data/seed.json | 种子数据（首次运行自动初始化为 data/stories.json） |
| data/stories.json | 运行时数据库（故事/评论/点赞/删除凭证都存在这里） |
| data/feedback.json | 网友反馈（自动生成） |
| data/views_log.json | 浏览量防刷记录（自动生成，IP 24h 去重） |
| Dockerfile / docker-compose.yml | Docker 部署用 |

---

## ⚠️ 部署前必改：管理密码

`api.php` 和 `server.js` 顶部都有：

```php
const ADMIN_KEY = 'yuanfen2025';   // ← 改成你自己的密码（两处保持一致）
```

管理页 `admin.html`（网址：`你的域名/admin.html`）用这个密码解锁，可以：
- 查看/删除网友反馈（含联系方式）
- 查看全部故事（按最新排序、按标签筛选）、删除任意故事

**不改密码 = 任何知道地址的人都能管理你的站点，务必修改！**

---

## 方式一：宝塔面板部署（推荐，最简单）

1. 宝塔面板 →「网站」→ 添加站点：
   - 域名：填你的域名或 `IP:端口`
   - PHP 版本：**7.4 及以上**（纯文件操作，不需要任何扩展、不需要数据库）
2. 上传以下文件到站点根目录（如 `/www/wwwroot/你的站点/`）：
   - `index.html`、`publish.html`、`admin.html`、`feedback.html`、`terms.html`、`privacy.html`、`disclaimer.html`
   - `api.php`
   - `data/` 整个文件夹
3. 设置 `data` 目录权限为 **755 或 775**（确保 www 用户可写）：
   - 宝塔文件管理 → 右键 data 目录 → 权限 → 勾选「写入」
4. 如果站点开了「防盗链/安全加固」，确认 `api.php` 允许 POST 请求。
5. 浏览器访问站点即可。想重新开始数据，删除 `data/stories.json`（保留 seed.json）刷新即可重置。

## 方式二：Docker 部署

```bash
# 上传整个项目目录到服务器（如 /www/yuanfen/），然后：
cd /www/yuanfen
docker compose up -d --build
# 访问 http://服务器IP:8010（端口在 docker-compose.yml 里改）
```

> 服务器如拉取 php:8.2-apache 镜像慢，可先给 Docker 配置国内镜像加速。

---

## 数据说明

- 所有数据存在 `data/` 目录，**备份该目录即备份全部数据**
- `data/stories.json`：故事/评论/点赞；`data/feedback.json`：网友反馈
- `server.js` 和 `api.php` 共用同一份数据文件，本地测试的数据部署后原样保留

## 内置防刷机制

- **浏览量**：同一 IP 对同一故事 24 小时内只计 1 次（自动识别反代真实 IP）+ 浏览器会话内去重
- **点赞**：浏览器本地记忆，不可连点
- **评论/反馈**：长度限制 + 服务端净化
- **用户 HTML**：服务端净化（去 script/事件属性/危险 CSS），`<style>` 样式保留但自动限定在故事容器内（`:root`/`body` 映射到容器、`100vh` 按弹窗比例换算、`position:fixed` 转 `absolute`），不影响站点本身

## 本地开发

```bash
node server.js        # 默认 http://localhost:8000（端口被占会自动 +1）
PORT=8020 node server.js  # 或指定端口
```

## API 一览（供后续开发参考）

统一入口 `api.php?route=xxx`：

| route | 方法 | 参数 | 说明 |
|-------|------|------|------|
| stories | GET | filter=hot/new/13 个分类, page, limit | 故事列表（热门按 点赞×3+评论×5+浏览×0.5 排序） |
| stories | POST | JSON body: {title, content, mode, tags, nickname} | 发布故事（返回一次性 editKey 删除凭证；HTML 服务端净化+样式作用域化） |
| story | GET | id, count | 故事详情；count=0 不计数（配合 IP 24h 去重防刷） |
| like | POST | JSON body: {id, undo} | 点赞 / 取消点赞 |
| comments | GET / POST | id; body: {nickname, content} | 评论列表 / 发表评论 |
| delete_story | POST | JSON body: {id, editKey} | 发布者凭凭证删除自己的故事 |
| tags | GET | — | 13 个分类的计数（首页标签云用） |
| stats | GET | — | 站点统计（故事数/点赞/浏览/甜蜜数） |
| hot | GET | limit | 本周热门 TOP N |
| feedback | POST | JSON body: {nickname, contact, content} | 网友提交反馈 |
| admin_feedback | POST | JSON body: {key, op: list/delete, id} | 管理：查看/删除反馈（需 ADMIN_KEY） |
| admin_stories | POST | JSON body: {key, op: list/delete, id, tag} | 管理：故事列表（最新在前、按标签筛选）/删除任意故事（需 ADMIN_KEY） |

## 页面一览

| 页面 | 地址 | 说明 |
|------|------|------|
| 首页 | `/` | 故事广场（筛选/详情/评论/点赞/关于/标签云） |
| 发布页 | `/publish.html` | 单选标签、纯文本/HTML 编辑、上传 HTML 文件、富文本粘贴 |
| 反馈建议 | `/feedback.html` | 网友提交反馈 |
| 站长管理 | `/admin.html` | 反馈箱 + 故事管理（需密码） |
| 内容规范 / 隐私声明 / 免责声明 | `/terms.html` 等 | 完整合规文案 |
