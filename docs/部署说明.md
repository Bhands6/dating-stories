# 缘分故事屋 💕

一个**零数据库依赖**的匿名相亲故事分享社区。单文件 PHP 后端 + 纯前端页面 + JSON 文件存储，2 核 2G 服务器轻松运行。

- **技术栈**：PHP 7.4+（生产）/ Node.js（本地开发）· 纯 HTML/CSS/JS 前端 · JSON 文件存储
- **无需**：MySQL、任何 PHP 扩展、构建工具、用户注册系统

---

## ✨ 功能特性

**前台（访客）**
- 故事广场：热门/最新 + 13 类标签筛选，分页加载
- 详情弹窗：点赞（本地记忆防连点）、匿名评论、评论回复（无限层级树形嵌套，「回复 @昵称」提示）
- HTML 排版故事：沙箱 iframe 渲染 + 「原文阅读」新窗口打开，样式 100% 原样
- 浏览量防刷：同 IP 24 小时去重
- 标签云（实时计数）、站点统计、关于与合规页面（内容规范/隐私声明/免责声明）

**发布（匿名，无需登录）**
- 13 类单选标签（默认「日常记录」）
- 纯文本 / HTML 双模式编辑，HTML 支持上传本地文件 + 富文本粘贴
- 发布后自动跳转预览自己的故事；随机匿名昵称
- 发布者获得删除凭证，可随时删除自己的故事

**站长管理（`/admin.html`，密码保护）**
- 📮 反馈箱：查看/删除网友反馈（含联系方式）
- 📚 故事管理：全部故事按最新排序、按标签筛选、点击预览（沙箱弹窗）、删除任意故事

**内置安全**
- 内容净化：去 script/事件属性/危险协议/CSS 表达式
- 样式隔离：HTML 故事以沙箱 iframe + CSP 沙箱头渲染，与站点完全隔离
- 管理接口密码校验；评论/反馈长度限制

---

## 📁 目录结构

```
dating-stories/
├── index.html              # 首页（故事广场/详情/评论/标签云/关于）
├── publish.html            # 发布页（双模式编辑/上传 HTML/富文本粘贴）
├── admin.html              # 站长管理页（密码保护）
├── feedback.html           # 反馈建议页
├── terms.html              # 内容规范
├── privacy.html            # 隐私声明
├── disclaimer.html         # 免责声明
├── api.php                 # PHP 后端（生产用，单文件零依赖）
├── server.js               # Node 开发服务器（接口与 api.php 对齐）
├── package.json
├── Dockerfile / docker-compose.yml / .dockerignore
├── 部署说明.md             # 本文件
└── data/                   # 数据目录（部署后自动生成，需可写）
    ├── seed.json           # 种子数据（在仓库中，首次运行初始化用）
    ├── stories.json        # 运行时生成：故事/评论/点赞/删除凭证
    ├── feedback.json       # 运行时生成：网友反馈
    └── views_log.json      # 运行时生成：浏览量防刷记录
```

---

## 🚀 部署

### ⚠️ 部署前必改：管理密码

`api.php` 和 `server.js` 顶部都有：

```php
const ADMIN_KEY = 'yuanfen2025';   // ← 改成你自己的密码（两处保持一致）
```

管理页 `admin.html` 用这个密码解锁。**不改密码 = 任何知道地址的人都能管理你的站点，务必修改！**

### 方式一：宝塔面板（推荐，最简单）

1. 宝塔 →「网站」→ 添加站点（域名或 `IP:端口`，PHP **7.4+**，不勾选数据库）
2. 上传全部文件到站点根目录（或服务器上 `git clone` 本仓库）
3. `data` 目录权限设为 **755/775**（确保 www 用户可写）
4. 若站点开了「防盗链/安全加固」，确认允许 `api.php` 的 POST 请求
5. 访问站点即可。想清空数据重来：删除 `data/stories.json`（保留 seed.json）即可重置

### 方式二：Docker

```bash
git clone https://github.com/Bhands6/dating-stories.git
cd dating-stories
docker compose up -d --build
# 访问 http://服务器IP:8010（端口在 docker-compose.yml 里改）
```

> 拉取 php:8.2-apache 镜像慢的话，先给 Docker 配国内镜像加速。

### 方式三：服务器直接 git 拉取（便于后续更新）

```bash
cd /www/wwwroot
git clone https://github.com/Bhands6/dating-stories.git
# 宝塔建站指向该目录即可
```

---

## 🔄 后续更新（本地改完 → 服务器生效）

```bash
# 本地
git add -A && git commit -m "更新说明" && git push

# 服务器
cd /www/wwwroot/dating-stories && git pull
```

> `data/` 下的运行时数据不在仓库里，更新代码不会影响已发布的内容。

---

## 💾 数据与备份

| 文件 | 内容 | 备份策略 |
|------|------|----------|
| `data/stories.json` | 故事/评论/点赞/删除凭证 | ⭐ 核心数据，定期下载备份 |
| `data/feedback.json` | 网友反馈 | 建议一并备份 |
| `data/views_log.json` | 浏览量防刷记录 | 可不备份（自动过期清理） |

- **全站备份 = 复制整个 `data/` 目录**；恢复 = 放回原位
- 本地（Node）与服务器（PHP）共用同一套数据格式，`data/` 直接拷贝即可迁移

---

## 💻 本地开发

```bash
node server.js             # http://localhost:8000（端口被占自动 +1）
PORT=8020 node server.js   # 指定端口
```

无需 npm install，零依赖。改完代码刷新浏览器即可（本地服务已禁用 HTML 缓存）。

---

## 📖 API 一览

统一入口 `api.php?route=xxx`（本地 Node 服务器同路径同参数）：

| route | 方法 | 参数 | 说明 |
|-------|------|------|------|
| stories | GET | filter=hot/new/13 分类, page, limit | 故事列表（热门按 点赞×3+评论×5+浏览×0.5） |
| stories | POST | {title, content, mode, tags, nickname} | 发布故事，返回一次性 editKey 删除凭证 |
| story | GET | id, count | 详情；count=0 不计数（配合 IP 24h 去重） |
| raw | GET | id | 故事原文（HTML 沙箱渲染 / 纯文本排版），CSP 沙箱头 |
| like | POST | {id, undo} | 点赞 / 取消 |
| comments | GET / POST | id; {nickname, content, replyTo?} | 评论列表 / 发表；replyTo 传目标评论或回复的 id 即为楼中楼回复（递归查找目标节点，回复挂其 replies 下，层级不限），评论数=种子基数+全部评论/回复节点 |
| delete_story | POST | {id, editKey} | 发布者凭凭证删除自己的故事 |
| tags | GET | — | 13 分类计数（标签云） |
| stats | GET | — | 站点统计 |
| hot | GET | limit | 本周热门 TOP N |
| feedback | POST | {nickname, contact, content} | 网友提交反馈 |
| admin_feedback | POST | {key, op: list/delete, id} | 管理：反馈查看/删除 |
| admin_stories | POST | {key, op: list/delete, id, tag} | 管理：故事列表（最新在前、标签筛选）/删除 |

---

## ❓ 常见问题

**Q：改了代码/样式，浏览器显示没变化？**
浏览器缓存了旧页面，按 `Ctrl + F5` 强制刷新。（服务端已禁用 HTML/JS 缓存，正常刷新也应拿到新版）

**Q：管理页进不去/提示密码错误？**
确认访问地址是 `域名/admin.html`；密码是 `api.php` 和 `server.js` 里的 `ADMIN_KEY`（部署时改过的那个）。忘记密码就上服务器看这两个文件。

**Q：想清空所有数据重新开始？**
删除服务器上的 `data/stories.json` 和 `data/feedback.json`（保留 `seed.json`），刷新即自动重置为示例内容。

**Q：上传的 HTML 故事样式不对/显示不全？**
请用「📄 上传 HTML 文件」方式导入（不要只复制纯文本），发布后通过弹窗里「📖 原文阅读」查看原样式效果。仍异常的话，检查文件里的 CSS 是否写在了 `<link>` 外链（外链样式不会随文件带入）。

**Q：发布后自动弹出的故事，刷新主页又弹？**
已修复：关闭弹窗时会自动清掉地址栏的一次性参数。若仍复现，确认部署的是最新版 `index.html`。

**Q：端口被占用？**
本地 `node server.js` 会自动从 8000 起尝试 +1；服务器端口在 `docker-compose.yml`（Docker）或宝塔站点设置（PHP）里改。

**Q：故事/评论能删吗？**
发布者：详情弹窗里「🗑 删除我的故事」（凭发布时自动保存的凭证，换浏览器/清缓存后凭证失效）。站长：管理页可删除任意故事和反馈。

---

## 📜 版本历史

**v1.0（2026-09-24）**
- 首页故事广场：热门/最新/13 类标签筛选、分页、详情弹窗、评论、点赞、标签云、统计
- 发布页：单选标签、纯文本/HTML 编辑、上传 HTML 文件（自动提取正文与样式表）、富文本粘贴、沙箱预览
- HTML 排版故事：沙箱 iframe + 原样式全屏阅读，样式 100% 原样（滚动动画自动驱动）
- 防刷：浏览量 IP 24h 去重、点赞本地记忆、内容净化、样式沙箱隔离
- 管理页：反馈箱 + 故事管理（最新排序、标签筛选、点击预览、删除）
- 合规页：内容规范/隐私声明/免责声明
- 部署：宝塔 PHP / Docker / git 拉取，零数据库依赖
